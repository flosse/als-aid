const
    ROSC = "message-when-alive",
    DEATH = "message-when-dead",
    CPR_START = "Thoracic compression: Started";
    CPR_STOP = "Thoracic compression: Stopped";
    APP_START = "Application started";

const MESSAGES = {
    ROSC,
    DEATH,
    CPR_START,
    CPR_STOP,
    APP_START,
}

exports.MESSAGES = MESSAGES;
